org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
# Pin the Gradle JVM to a supported JDK. AGP 9.x + Gradle 9.3.1 crash on JDK 26
# ("core-for-system-modules" jlink transform), so force the stable JDK 21 that
# ships with the VS Code Java extension pack. Forward slashes are required.
org.gradle.java.home=C:/Users/tumma/AppData/Roaming/Code/User/globalStorage/pleiades.java-extension-pack-jdk/java/21
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
